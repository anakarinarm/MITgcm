# LR analytical M2 forcing: south A0=0.01 m/s, north-only return

This is the lower-amplitude companion to `runBC_LR_analyticM2_south_A002`.

Architecture:

- no SSH;
- south normal M2 velocity with `A0 = 0.01 m/s`;
- 10-cell cosine taper near south corners;
- north uniform compensating return flow;
- west prescribed velocity exactly zero;
- Stevens/radiative W/S/N;
- strong 20-cell momentum sponge.

Generated by:

```bash
python3 prepare_analytic_south_tide_A001.py
```

Generation diagnostics:

- south max/RMS amplitude: `0.0100 / 0.00968 m/s`
- north return max/RMS amplitude: `0.02145 / 0.01617 m/s`
- west max/RMS amplitude: `0.0 / 0.0 m/s`
- transport residual `Q_W + Q_S - Q_N`: `0.0 m3/s`

Run and analyze:

```bash
mpiexec -n 16 ./mitgcmuv
python3 check_boundary_reflections.py
python3 modal_characteristic_decomposition.py
```

Compare against:

- `../runBC_LR_analyticM2_south_A002`
- `../runBC_LR_analyticM2_south_A001_NWreturn`
- `../runBC_LR_noSSH_noW_strongSponge`

## Result

The run completed normally; STDERR only contained the harmless retired
`EXACT_CONSERV` warning.

This run lands very close to the noW TPXO energy scale, but with much cleaner
ETAN.

At offset 22 cells:

| boundary | mode-1 outward/inward | incoming fraction | bulk flux |
|---|---:|---:|---:|
| W | `4.38 / 4.55 MW` | `0.509` | `+0.838 MW` |
| S | `11.41 / 3.72 MW` | `0.246` | `+9.99 MW` |
| N | `0.41 / 0.26 MW` | `0.390` | `+0.732 MW` |

West mode 1 at offset 33 was `3.91 / 5.80 MW`, incoming fraction `0.597`.

Final ETAN:

- min/max: `-0.0118 / +0.0069 m`
- mean: `+0.00009 m`
- RMS: `0.00289 m`

Interpretation: this is currently the cleanest analytical forcing candidate
that still produces south mode-1 energy comparable to the noW TPXO run.  The
north return is visible but not catastrophic, and ETAN is much cleaner than any
TPXO-derived case.
